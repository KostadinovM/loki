#include "camera.h"
namespace Loki
{
	namespace Graphics
	{
		Camera::Camera()
		{
		}

		Camera::Camera(glm::vec3 position, glm::vec3 forward, glm::vec3 up)
		{
		}

		void Camera::UpdateViewMatrix()
		{
		}
	}
}